# ========================================
# Local Development Settings
# ========================================
FLASK_ENV=development
SECRET_KEY=dev-secret-key-change-in-production
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/todo_dev

# ========================================
# Testing Settings (for pytest)
# ========================================
# Note: TestingConfig ใช้ SQLite in-memory, ไม่จำเป็นต้องแก้ .env
# TESTING_DB_URL=sqlite:///:memory:

# ========================================
# Production Settings (for deployment)
# ========================================
# SECRET_KEY ต้องปลอดภัยมาก และไม่ควรใช้ค่า default
# DATABASE_URL ต้องระบุ host, user, password, port, db name จริง
# Uncomment และแก้ค่าตาม environment ของคุณ
# FLASK_ENV=production
# SECRET_KEY=your-production-secret-key
# DATABASE_URL=postgresql://username:password@host:port/production_db

# ========================================
# Optional: Other Environment Variables
# ========================================
# DEBUG mode override
# DEBUG=False
# Additional configs for third-party services (optional)
# MAIL_SERVER=smtp.example.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email
# MAIL_PASSWORD=your-email-password
